# Copy to .env.local to override. Vite inlines VITE_* vars at build time.
#
# Leave unset to use the relative default (/api/v1), which the dev server
# proxies to http://localhost:8000 and which is same-origin in production.
# VITE_API_BASE=http://localhost:8000/api/v1

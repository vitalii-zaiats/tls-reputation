---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart tlsrep-backend
  ansible.builtin.systemd_service:
    name: tlsrep-backend
    state: restarted
    daemon_reload: true
